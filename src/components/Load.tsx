import { IconLoader } from "@tabler/icons-react";

export default function Load() {
    return (
        <div className="self-center mt-[30vh]">
            <IconLoader size={50} className="animate-spin" />
        </div>
    )
}